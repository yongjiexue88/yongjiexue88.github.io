Separate groups when whitespace alone isn't enough.

```jsx
<Divider label="or" />
<Divider vertical />
```

Notes
- One hairline color (`--lg-border-subtle`) across the whole system.
- Prefer space over rules; a screen with four dividers usually needs fewer sections.

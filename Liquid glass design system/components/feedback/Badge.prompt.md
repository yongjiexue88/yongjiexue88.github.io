State attached to something else: a row's status, a count, a label.

```jsx
<Badge tone="success" dot>Published</Badge>
<Badge>Draft</Badge>
```

Notes
- One or two words, sentence case, no punctuation.
- Never glass — a badge is too small to refract anything legibly.
- If the user can remove it, it is a Tag, not a Badge.

Names an icon-only control, or expands a truncated value.

```jsx
<Tooltip content="Add to library" side="bottom">
  <IconButton variant="ghost" label="Add"><Icon name="plus" /></IconButton>
</Tooltip>
```

Notes
- Never put essential information here — it is invisible on touch.
- Does not wrap; keep content to a few words.
